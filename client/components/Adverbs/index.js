import React from 'react';

import { Card, CardContent, CardTitle } from '../share';

const propTypes = {

};

const defaultProps = {

};

const Adverbs= () => (
  <Card >
    <CardContent>
      <CardTitle>
        Adverbs
      </CardTitle>
      <ul>
        <li>
          S + V(preteritum) + O.
        </li>
      </ul>
    </CardContent>
  </Card>
);

Adverbs.propTypes = propTypes;
Adverbs.defaultProps = defaultProps;

export default Adverbs;
